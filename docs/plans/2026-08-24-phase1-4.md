# job-scouter Phase 1~4 Implementation Plan
> 2026-08-26 사설 디렉터리에서 이전. 개인 인프라 정보(LAN 주소·절대경로·데이터 repo 이름·계정)는 중립 표기로 치환했다. 치환 전 원본은 저장소 밖에 남아 있다.


> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (프로젝트 규칙: task당 서브에이전트 1개, 리뷰 세리머니 생략) to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 수동 job-scout 사이클을 Temporal durable workflow 위의 agent 판정 파이프라인으로 승격 — 공고 채점(judge)을 자동화하고, 사람이 끼는 두 지점(브라우저 평판 조사, 등재 승인)은 signal 대기로 모델링한다.

**Architecture:** 서버(<server-ip>)의 **OrbStack 컨테이너**(`temporalio/temporal` 이미지, `server start-dev`, SQLite 볼륨 영속, :7233/:8233 — 2026-08-24 사용자 결정으로 launchd에서 변경)가 서버, 작업 머신이 워커. 주 1회 Temporal Schedule이 사이클을 자동 시작한다(기존 launchd fetch 대체). 큐 3개(`jobscout-wf`/`jobscout-io`/`jobscout-llm`)는 자격증명 격리 경계 — ANTHROPIC 키는 llm 워커 프로세스에만 존재한다. judge는 공고 1건 = activity 1개(단일 forced-tool 호출), 캐시키 `(공고id, 루브릭버전, 사실베이스해시)`. Phase 3에서 서버 ES(PKB, 8.17)에 jobscout 인덱스 3종을 추가하고 bge-m3 + BM25+kNN RRF(리랭커 없음, k=20)로 검색 컨텍스트를 주입한다.

**Tech Stack:** Python 3.11 (uv), temporalio, anthropic, elasticsearch + sentence-transformers(bge-m3, Phase 3), 기존 jobfeed 스크립트(fetch_jobs/refresh_due/build — 수정하지 않고 subprocess로 감쌈).

**Spec:** `DESIGN.md` (repo 루트). 루브릭 원본: `${DATA_REPO}/.claude/skills/job-scout/SKILL.md` §5~6.

## Global Constraints

- **서버에서의 실행은 전부 사용자가 직접 한다.** 에이전트는 `deploy/` 아래 파일·명령만 준비하고, 사용자 실행 완료 후 작업 머신에서 검증한다. (사용자 결정 2026-08-24)
- **workflow 코드는 결정론적** — 시계·랜덤·파일IO·네트워크 금지, 전부 activity로.
- **ANTHROPIC_API_KEY는 llm 워커 프로세스에만.** `jobscouter/judge.py` 외의 모듈은 anthropic을 import하지 않는다 (Task 4에서 테스트로 강제).
- **judge 캐시키 = `(공고id, 루브릭버전, 사실베이스 sha256)`** — 사실베이스가 바뀌면 자동 재판정 대상.
- **루브릭 = `prompts/rubric_v1.md`**, git 버전 관리. 모든 판정 레코드에 `rubric_version` 스탬프.
- **폭주 방지는 타입·설정으로**: judge `max_tokens`는 activity 파라미터, 레이트리밋은 llm 큐 레벨(`max_task_queue_activities_per_second=0.5`), 사이클 예산 초과 시 잔여 공고는 미점수로 강등(판정 안 함 — build.py가 "미점수 N건"으로 이미 보고).
- **RAG**: 서버 기존 ES(:9200)에 인덱스 추가만. 임베딩 `BAAI/bge-m3`(1024d, cosine — PKB 실측 채택 모델), BM25+kNN 각각 검색 후 **클라이언트 RRF 융합**(PKB `retrieve.py` 패턴, ES 라이선스 무관), 리랭커 없음, k=20.
- **데이터 경계 (DESIGN §데이터 경계)**: 시스템이 읽는 개인 데이터는 사실베이스·판정 판례·기업평판 캐시 **3종뿐**. job-scouter는 자기 인덱스(`jobscout_*`)만 만들고 **PKB 인덱스(`pkb_documents`)를 절대 읽지 않는다.** JK.md·applications/·지원이력 원문도 읽지 않는다. Anthropic API 호출은 llm 큐 activity만. :7233/:8233/:9200은 LAN 밖으로 열지 않는다.
- **jobfeed 스크립트(`fetch_jobs.py`/`refresh_due.py`/`build.py`)는 수정하지 않는다.** 검증 게이트는 build.py 그대로. 실행 인터프리터는 `/Library/Frameworks/Python.framework/Versions/3.11/bin/python3` (에이전트 셸 python3는 certifi가 없어 API 호출이 죽는다 — jobfeed 실측).
- **candidates.json 행 형식** (build.py `check()`가 검증): `[포지션, 회사, id, [스택≤35,도메인≤25,레벨≤20,역할≤20,감점-25~0], 평판|null, 평판없음사유|null, tags[], 마감]` — 새 행은 8필드(마감·근무지는 refresh_due.py가 채움). id는 원티드 int, 점핏 `"j{id}"` str. 평판 null이면 사유 필수.
- **테스트는 ponytail 규칙**: 유닛당 최소 1개 러너블 체크. TDD 세리머니·픽스처 프레임워크 없음. `uv run pytest -q`로 전부 돈다.
- 커밋은 이 저장소(job-scouter)에서 task마다 1회. ${DATA_REPO} repo는 Phase 4의 commit_rows activity만 건드린다.

## 확인된 환경 사실 (2026-08-24 실측)

| 항목 | 값 |
|---|---|
| 서버 | `ssh <server>` = <user>@<server-ip>, brew 있음(/opt/homebrew), temporal 미설치, :7233/:8233 비어 있음, launchd 패턴 `dev.<user>.*` |
| 서버 OrbStack | **설치·상주 확인** (OrbStack.app 실행 중, vmgr 동작). CLI는 `~/.orbstack/bin`(docker·docker-compose·orbctl) — 비대화 ssh 셸 PATH에 없으므로 전체 경로 또는 PATH 추가 필요 |
| 서버 ES | 8.17.0, cluster `pkb`, `~/opt/elasticsearch`, **network.host: 127.0.0.1** (Phase 3에서 LAN 개방 필요), launchd `dev.<user>.pkb-es` |
| 작업 머신 | uv 0.8.4, Python 3.11.8, temporal CLI 1.8.2 설치됨, ANTHROPIC_API_KEY 셸에 없음(.env 필요) |
| jobfeed | `${DATA_REPO}/jobfeed/` — candidates.json rows 161(9필드)·skipped 549(dict id→[회사,포지션,사유]), jobs.jsonl 누적, 기업평판.md 43KB |
| 사실베이스 | `${DATA_REPO}/references/이력서_사실베이스.md` 42.5KB |
| Phase 0 데모 | `${DATA_REPO}/jobfeed/temporal_flow.py` — fetch→signal→refresh→build COMPLETED 확인 |

## File Structure

```
job-scouter/
  DESIGN.md                  (기존)
  pyproject.toml             T1
  .gitignore                 T1
  .env                       T4 (사용자가 키 기입, gitignore)
  jobscouter/
    __init__.py              T1
    config.py                T1 (상수 + 공유 dataclass — 이후 task가 필드 추가)
    io_acts.py               T1 run_script → T3 fetch_requirements/load_targets → T9 commit_rows
    workflow.py              T1 골격 → T5 judge 통합 → T9 승인·커밋 → T10 report
    worker.py                T1 CLI (worker/run/signal/status) → T11 schedule·핸들 해석
    judge.py                 T4 (llm 큐 — anthropic은 이 파일만)
    search.py                T7 (ES 색인·하이브리드 검색) → T8 search_context activity
  prompts/rubric_v1.md       T4
  scripts/
    eval_judge.py            T6 (Phase 2 게이트) → T8 --rag 플래그
    index_es.py              T7
  deploy/
    compose.yaml             T2 (OrbStack용 — temporal 단일 서비스)
    MINI_SETUP.md            T2 (temporal 절) → T7 (ES LAN 개방 절 추가)
  tests/
    test_workflow.py         T1 → T5 → T9 확장
    test_io_acts.py          T3 → T9 확장
    test_judge.py            T4
  data/                      (gitignored — judgments.jsonl, eval 결과)
```

---

# Phase 1 — 인프라: 서버 서버 + 저장소 뼈대 + 워커 전환

### Task 1: 저장소 뼈대 + Phase 0 워크플로 이식

**Files:**
- Create: `pyproject.toml`, `.gitignore`, `jobscouter/__init__.py`, `jobscouter/config.py`, `jobscouter/io_acts.py`, `jobscouter/workflow.py`, `jobscouter/worker.py`
- Test: `tests/test_workflow.py`

**Interfaces:**
- Consumes: 없음 (첫 task)
- Produces:
  - `config.py`: `TEMPORAL: str`, `JOBFEED: Path`, `FACTBASE: Path`, `DATA: Path`, `PROMPTS: Path`, `PY: str`, `Q_WF/Q_IO/Q_LLM: str`, `WORKFLOW_ID: str`
  - `io_acts.run_script(name: str) -> str` (activity)
  - `workflow.JobScoutCycle` — `run(params: CycleParams) -> dict`, signal `browser_done(note: str)`, query `status() -> dict`
  - `config.CycleParams` dataclass: `browser_wait_minutes: int = 120`
  - `worker.py` CLI: `python -m jobscouter.worker io|run|browser-done|status`

- [ ] **Step 1: git init + 프로젝트 파일**

```bash
cd <repo>
git init -b main
```

`pyproject.toml`:
```toml
[project]
name = "jobscouter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "temporalio>=1.9",
]

[dependency-groups]
dev = ["pytest>=8", "pytest-asyncio>=0.23"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
```

`.gitignore`:
```
.env
data/
__pycache__/
.venv/
*.egg-info/
```

- [ ] **Step 2: config.py**

```python
"""공유 상수·타입. 워커 배치는 env로 바꾼다 — 코드 수정 없이."""
import os
from dataclasses import dataclass, field
from pathlib import Path

TEMPORAL = os.environ.get("JOBSCOUTER_TEMPORAL", "<server-ip>:7233")
JOBFEED = Path(os.environ.get(
    "JOBSCOUTER_JOBFEED", "${DATA_REPO}/jobfeed"))
FACTBASE = JOBFEED.parent / "references" / "이력서_사실베이스.md"
ROOT = Path(__file__).resolve().parent.parent
DATA = ROOT / "data"
PROMPTS = ROOT / "prompts"
# 에이전트 셸 python3는 certifi가 없어 원티드 API 호출이 죽는다 (jobfeed 실측)
PY = "/Library/Frameworks/Python.framework/Versions/3.11/bin/python3"
Q_WF, Q_IO, Q_LLM = "jobscout-wf", "jobscout-io", "jobscout-llm"
WORKFLOW_ID = "job-scout-cycle"


@dataclass
class CycleParams:
    browser_wait_minutes: int = 120
```

- [ ] **Step 3: io_acts.py — Phase 0의 run_script 이식**

```python
"""io 큐 activity — 자격증명 없음. jobfeed 스크립트 subprocess + 공개 API."""
import subprocess

from temporalio import activity

from jobscouter.config import JOBFEED, PY


@activity.defn
def run_script(name: str) -> str:
    """jobfeed 스크립트 하나를 돌리고 출력 꼬리를 돌려준다."""
    r = subprocess.run([PY, str(JOBFEED / name)], capture_output=True, text=True,
                       timeout=540, cwd=JOBFEED)
    tail = "\n".join((r.stdout + r.stderr).strip().splitlines()[-6:])
    if r.returncode != 0:
        raise RuntimeError(f"{name} exit {r.returncode}\n{tail}")
    return tail
```

- [ ] **Step 4: workflow.py — Phase 1 골격 (fetch → browser signal → refresh → build)**

```python
"""JobScoutCycle — 결정론적 오케스트레이션만. 파일IO·네트워크·시계 직접 사용 금지."""
from datetime import timedelta

from temporalio import workflow
from temporalio.common import RetryPolicy

from jobscouter.config import Q_IO, CycleParams

_IO_OPTS = dict(
    task_queue=Q_IO,
    start_to_close_timeout=timedelta(minutes=10),
    retry_policy=RetryPolicy(maximum_attempts=3),
)


@workflow.defn
class JobScoutCycle:
    def __init__(self) -> None:
        self._browser_note: str | None = None
        self._stage = "시작"

    @workflow.signal
    def browser_done(self, note: str) -> None:
        self._browser_note = note or "완료"

    @workflow.query
    def status(self) -> dict:
        return {"stage": self._stage, "browser": self._browser_note}

    @workflow.run
    async def run(self, params: CycleParams) -> dict:
        out: dict = {}

        self._stage = "fetch"
        out["fetch"] = await workflow.execute_activity(
            "run_script", "fetch_jobs.py", **_IO_OPTS)

        # 수동 반쪽: 원티드 매칭·잡플래닛은 로그인 크롬 필요 — durable 대기
        self._stage = "브라우저 조사 대기"
        try:
            await workflow.wait_condition(
                lambda: self._browser_note is not None,
                timeout=timedelta(minutes=params.browser_wait_minutes))
            out["browser"] = self._browser_note
        except TimeoutError:
            out["browser"] = f"타임아웃({params.browser_wait_minutes}분) — 수동 단계 생략"

        self._stage = "refresh"
        out["refresh"] = await workflow.execute_activity(
            "run_script", "refresh_due.py", **_IO_OPTS)
        self._stage = "build"
        out["build"] = await workflow.execute_activity(
            "run_script", "build.py", **_IO_OPTS)
        self._stage = "완료"
        return out
```

activity를 문자열 이름(`"run_script"`)으로 호출하는 이유: workflow 모듈이 io/llm 모듈을 import하지 않아야 llm 워커·샌드박스에 io 의존성이 끌려가지 않는다.

- [ ] **Step 5: worker.py — CLI**

```python
"""실행 진입점.

    uv run python -m jobscouter.worker io            # workflow+io 워커 (자격증명 없음)
    uv run python -m jobscouter.worker run           # 사이클 시작
    uv run python -m jobscouter.worker browser-done "메모"
    uv run python -m jobscouter.worker status
"""
import asyncio
import sys
from concurrent.futures import ThreadPoolExecutor

from temporalio.client import Client
from temporalio.worker import Worker

from jobscouter.config import TEMPORAL, WORKFLOW_ID, Q_IO, Q_WF, CycleParams


async def main() -> None:
    cmd = sys.argv[1] if len(sys.argv) > 1 else "status"
    client = await Client.connect(TEMPORAL)

    if cmd == "io":
        from jobscouter import io_acts
        from jobscouter.workflow import JobScoutCycle
        acts = [io_acts.run_script]
        ex = ThreadPoolExecutor(4)
        workers = [
            Worker(client, task_queue=Q_WF, workflows=[JobScoutCycle]),
            Worker(client, task_queue=Q_IO, activities=acts, activity_executor=ex),
        ]
        print(f"io 워커 시작 — {TEMPORAL} / {Q_WF}, {Q_IO}")
        await asyncio.gather(*(w.run() for w in workers))

    elif cmd == "run":
        from jobscouter.workflow import JobScoutCycle
        handle = await client.start_workflow(
            JobScoutCycle.run, CycleParams(),
            id=WORKFLOW_ID, task_queue=Q_WF)
        print(f"시작: {handle.id} → http://{TEMPORAL.split(':')[0]}:8233")

    elif cmd == "browser-done":
        note = sys.argv[2] if len(sys.argv) > 2 else "완료"
        await client.get_workflow_handle(WORKFLOW_ID).signal("browser_done", note)
        print("signal 전송:", note)

    elif cmd == "status":
        h = client.get_workflow_handle(WORKFLOW_ID)
        print(await h.query("status"))

    else:
        sys.exit(f"모르는 명령: {cmd}")


if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 6: 테스트 — 시그널 경로와 타임아웃 경로**

`tests/test_workflow.py`:
```python
"""워크플로 로직 체크 — activity는 스텁, 서버는 time-skipping 테스트 환경."""
import uuid

import pytest
from temporalio import activity
from temporalio.client import Client
from temporalio.testing import WorkflowEnvironment
from temporalio.worker import Worker

from jobscouter.config import CycleParams
from jobscouter.workflow import JobScoutCycle

RAN: list[str] = []


@activity.defn(name="run_script")
async def fake_run_script(name: str) -> str:
    RAN.append(name)
    return f"ok:{name}"


async def _run_cycle(client: Client, send_signal: bool) -> dict:
    q = f"test-{uuid.uuid4()}"
    async with Worker(client, task_queue=q, workflows=[JobScoutCycle],
                      activities=[fake_run_script]):
        # 테스트에서는 wf·io를 한 큐로 — 큐 분리는 운영 배치의 문제지 로직의 문제가 아니다
        import jobscouter.workflow as wf
        wf._IO_OPTS["task_queue"] = q
        handle = await client.start_workflow(
            JobScoutCycle.run, CycleParams(browser_wait_minutes=1),
            id=f"wf-{uuid.uuid4()}", task_queue=q)
        if send_signal:
            await handle.signal(JobScoutCycle.browser_done, "수동 완료")
        return await handle.result()


@pytest.mark.asyncio
async def test_signal_and_timeout_paths():
    env = await WorkflowEnvironment.start_time_skipping()
    try:
        RAN.clear()
        out = await _run_cycle(env.client, send_signal=True)
        assert out["browser"] == "수동 완료"
        assert RAN == ["fetch_jobs.py", "refresh_due.py", "build.py"]

        out = await _run_cycle(env.client, send_signal=False)
        assert "타임아웃" in out["browser"]  # 대기 무한 아님 — 강등 경로
    finally:
        await env.shutdown()
```

- [ ] **Step 7: 실행 확인**

```bash
cd <repo>
uv sync
uv run pytest -q          # 첫 실행은 테스트 서버 바이너리 다운로드로 수십 초
```
Expected: `2 passed` (테스트 함수 1개, 경로 2개 검증) — 정확히는 `1 passed`.

- [ ] **Step 8: Commit**

```bash
git add -A && git commit -m "Phase 1: 저장소 뼈대 + JobScoutCycle 골격 (Phase 0 이식)"
```

---

### Task 2: 서버 배포 번들(OrbStack) + 워커 전환 검증

**Files:**
- Create: `deploy/compose.yaml`, `deploy/MINI_SETUP.md`

**Interfaces:**
- Consumes: Task 1의 worker CLI
- Produces: 서버 :7233(서버)·:8233(UI) 가동 상태. 이후 모든 task는 `JOBSCOUTER_TEMPORAL` 기본값(<server-ip>:7233)으로 동작.

- [ ] **Step 1: compose.yaml 작성**

`deploy/compose.yaml`:
```yaml
# 서버 OrbStack에서 구동 (2026-08-24 결정 — OrbStack 상주 확인, DESIGN §배포)
# temporalio/temporal = CLI 이미지 (entrypoint가 temporal 바이너리)
services:
  temporal:
    image: temporalio/temporal:latest
    command: server start-dev --ip 0.0.0.0 --db-filename /data/temporal.db --log-level warn
    ports:
      - "7233:7233"   # gRPC — 작업 머신 워커
      - "8233:8233"   # UI
    volumes:
      - ~/.temporal:/data   # SQLite 영속 — 컨테이너 재생성에도 이력 유지
    restart: always                  # OrbStack 상주 + 부팅 시 자동 복구
```

- [ ] **Step 2: MINI_SETUP.md 작성 (사용자 실행용)**

````markdown
# 서버 설정 — 사용자가 직접 실행

## 1. Temporal 서버 (Phase 1, OrbStack)

작업 머신에서 compose 파일 복사:
```bash
scp deploy/compose.yaml <server>:jobscouter-compose.yaml
```

서버에서 (ssh <server> — 비대화 셸에는 OrbStack PATH가 없다):
```bash
export PATH="$HOME/.orbstack/bin:$PATH"
mkdir -p ~/.temporal
docker compose -f ~/jobscouter-compose.yaml up -d
docker ps --filter name=temporal    # Up 상태 확인
```

작업 머신에서 검증:
```bash
temporal operator cluster health --address <server-ip>:7233   # SERVING
open http://<server-ip>:8233                                  # UI
```

관리: `docker compose -f ~/jobscouter-compose.yaml restart` (재시작) /
`logs temporal` (로그). SQLite가 볼륨(`~/.temporal/temporal.db`)에 있어
컨테이너를 지워도 워크플로 이력·대기 상태가 유지된다. `restart: always`라
서버 재부팅 시 OrbStack이 자동 복구한다.

**주의**: :7233/:8233은 LAN 전용이다 — 포트포워딩·외부 공개 금지
(Temporal 이력에 사실베이스 발췌 사본이 남는다, DESIGN §데이터 경계).
````

(ES LAN 개방 절은 Task 7에서 이 파일에 추가한다.)

- [ ] **Step 3: 사용자에게 실행 요청 — BLOCKING**

사용자에게 MINI_SETUP.md §1 실행을 요청하고 완료 답을 기다린다. **사용자 확인 전에는 Step 4로 넘어가지 않는다.**

- [ ] **Step 4: 전환 검증 (작업 머신에서, 사용자 완료 후)**

```bash
temporal operator cluster health --address <server-ip>:7233
cd <repo>
uv run python -m jobscouter.worker io &      # 워커 (<server-ip>:7233 기본값)
uv run python -m jobscouter.worker run
uv run python -m jobscouter.worker status    # {'stage': '브라우저 조사 대기', ...}
kill %1                                      # 워커 죽임 — durable 확인
uv run python -m jobscouter.worker status    # 서버가 서버라 여전히 조회됨
uv run python -m jobscouter.worker io &      # 워커 재기동
uv run python -m jobscouter.worker browser-done "phase1 검증"
sleep 30 && uv run python -m jobscouter.worker status   # {'stage': '완료'} 확인 후
kill %1
```
Expected: 워커가 죽어 있는 동안에도 워크플로는 서버에서 대기 유지(= Phase 1 완료 판정), 재기동 후 signal로 refresh→build까지 완주. UI(http://<server-ip>:8233) 타임라인에서 확인.

주의: `run`은 실제 `fetch_jobs.py`를 돌려 jobs.jsonl에 누적한다 — 정상 동작이며 부작용 아님(수동 사이클과 동일).

- [ ] **Step 5: Commit**

```bash
git add deploy && git commit -m "Phase 1: 서버 temporal OrbStack 번들 + 전환 검증 절차"
```

---

# Phase 2 — judge activity (RAG 없이 사실베이스 전문 주입)

### Task 3: 판정 대상 선정 + 자격요건 수집 (io)

**Files:**
- Modify: `jobscouter/config.py` (Target dataclass 추가), `jobscouter/io_acts.py`
- Test: `tests/test_io_acts.py`

**Interfaces:**
- Consumes: `config.JOBFEED`
- Produces:
  - `config.Target` dataclass: `id: str`(원티드 `"365172"`, 점핏 `"j54800311"`), `company: str`, `title: str`, `src: str`("wanted"|"jumpit"), `url: str`
  - `io_acts.load_targets() -> list[Target]` (activity) — jobs.jsonl 전 건에서 rows·skipped·🚫 제외한 **미점수 전부** (신규+백로그)
  - `io_acts.fetch_requirements(target: Target) -> str` (activity) — 자격요건 원문, 300자 캡, 실패 시 예외(Temporal 재시도)

- [ ] **Step 1: config.py에 Target 추가**

```python
@dataclass
class Target:
    id: str        # 원티드 "365172" | 점핏 "j54800311" — candidates.json 관례
    company: str
    title: str
    src: str       # "wanted" | "jumpit"
    url: str
```

- [ ] **Step 2: io_acts.py에 두 activity 추가**

```python
import json
import re
import ssl
import urllib.request

from jobscouter.config import Target

try:  # python.org 빌드 대비 — fetch_jobs.py와 동일 처리
    import certifi
    _SSL = ssl.create_default_context(cafile=certifi.where())
except ImportError:
    _SSL = None
_HDR = {"User-Agent": "Mozilla/5.0", "wanted-os": "web"}
REQ_CAP = 300  # DESIGN: requirements 300자 캡


def _norm(s: str) -> str:
    """fetch_jobs.py의 회사명 정규화와 동일해야 제외 집합이 맞는다."""
    s = re.sub(r"\(주\)|주식회사|㈜|Inc\.?|Ltd\.?", "", s)
    s = re.sub(r"\([^)]*\)", "", s)
    return re.sub(r"\s+", "", s).lower()


def _get(url: str) -> dict:
    req = urllib.request.Request(url, headers=_HDR)
    with urllib.request.urlopen(req, timeout=20, context=_SSL) as r:
        return json.load(r)


@activity.defn
def load_targets() -> list[Target]:
    """jobs.jsonl − (rows ∪ skipped ∪ 🚫회사) = 미점수 전부."""
    cand = json.loads((JOBFEED / "candidates.json").read_text())
    known = {str(r[2]) for r in cand["rows"]} | set(cand["skipped"])
    bad = {_norm(r[1]) for r in cand["rows"] if r[4] and r[4][0] == "bad"}
    rep = JOBFEED / "기업평판.md"
    if rep.exists():
        for ln in rep.read_text().splitlines():
            if "🚫" in ln and ln.startswith("|") and len(ln.split("|")) > 1:
                bad.add(_norm(ln.split("|")[1].strip()))

    out, seen = [], set()
    for line in (JOBFEED / "jobs.jsonl").read_text().splitlines():
        j = json.loads(line)
        cid = str(j["id"]) if j["src"] == "wanted" else f"j{j['id']}"
        if cid in known or cid in seen or _norm(j["company"]) in bad:
            continue
        seen.add(cid)
        out.append(Target(id=cid, company=j["company"], title=j["title"],
                          src=j["src"], url=j["url"]))
    return out


@activity.defn
def fetch_requirements(t: Target) -> str:
    """자격요건 원문. 원티드: detail.requirements / 점핏: qualifications(복수!)+responsibility 첫 2줄.
    점핏 단수 qualification은 조용히 None — '필수요건 없음' 오독 사고 이력(SKILL.md)."""
    if t.src == "wanted":
        d = _get(f"https://www.wanted.co.kr/api/v4/jobs/{t.id}")
        text = (d["job"]["detail"].get("requirements") or "").strip()
    else:
        d = _get(f"https://jumpit-api.saramin.co.kr/api/position/{t.id[1:]}")
        r = d["result"]
        resp = "\n".join((r.get("responsibility") or "").splitlines()[:2])
        text = f"{(r.get('qualifications') or '').strip()}\n[주요업무 발췌] {resp}".strip()
    if not text:
        raise RuntimeError(f"{t.id}: 자격요건 없음 — 공고 내려갔거나 API 변경")
    return text[:REQ_CAP]
```

- [ ] **Step 3: 테스트 — 제외 집합·점핏 복수 키·300자 캡**

`tests/test_io_acts.py`:
```python
import json

import jobscouter.io_acts as io_acts
from jobscouter.config import Target


def _touch_jobfeed(tmp_path, monkeypatch):
    monkeypatch.setattr(io_acts, "JOBFEED", tmp_path)
    (tmp_path / "candidates.json").write_text(json.dumps({
        "rows": [["포지션", "나쁜회사", 111, [30, 10, 16, 20],
                  ["bad", 1.0, 10, "1,1", "요약"], None, [], None],
                 ["포지션", "등재회사", 222, [30, 10, 16, 20], None, "사유", [], None]],
        "skipped": {"333": ["스킵회사", "포지션", "사유"]},
    }, ensure_ascii=False))
    jobs = [
        {"src": "wanted", "id": 111, "title": "A", "company": "나쁜회사", "url": "u"},
        {"src": "wanted", "id": 222, "title": "B", "company": "등재회사", "url": "u"},
        {"src": "wanted", "id": 333, "title": "C", "company": "스킵회사", "url": "u"},
        {"src": "wanted", "id": 444, "title": "D", "company": "(주)나쁜회사", "url": "u"},
        {"src": "jumpit", "id": 555, "title": "E", "company": "새회사", "url": "u"},
    ]
    (tmp_path / "jobs.jsonl").write_text(
        "\n".join(json.dumps(j, ensure_ascii=False) for j in jobs))


def test_load_targets(tmp_path, monkeypatch):
    _touch_jobfeed(tmp_path, monkeypatch)
    got = io_acts.load_targets.__wrapped__() if hasattr(
        io_acts.load_targets, "__wrapped__") else io_acts.load_targets()
    ids = [t.id for t in got]
    # 111 등재됨·222 등재됨·333 스킵·444 🚫회사 정규화 매칭 → 미점수는 j555뿐
    assert ids == ["j555"]


def test_fetch_requirements_jumpit_plural_and_cap(monkeypatch):
    monkeypatch.setattr(io_acts, "_get", lambda url: {
        "result": {"qualifications": "필수: " + "가" * 400,
                   "qualification": None,  # 단수 키 함정 — 이걸 읽으면 None
                   "responsibility": "줄1\n줄2\n줄3"}})
    t = Target(id="j555", company="새회사", title="E", src="jumpit", url="u")
    text = io_acts.fetch_requirements(t)
    assert text.startswith("필수: 가")
    assert len(text) <= io_acts.REQ_CAP
    assert "줄3" not in text  # responsibility는 첫 2줄만 (캡 안에 들어올 때)
```

주의: `@activity.defn`이 함수를 그대로 노출하므로(`__wrapped__` 불필요하면 직접 호출) 실제 동작을 보고 단순한 쪽으로 정리한다 — temporalio는 데코레이트된 함수를 평범하게 호출할 수 있게 둔다.

- [ ] **Step 4: 실행 + 라이브 스모크 1건**

```bash
uv run pytest -q tests/test_io_acts.py
# 라이브 확인 (원티드 공고 1건 — candidates.json의 실존 id로):
uv run python -c "
from jobscouter.config import Target
from jobscouter.io_acts import fetch_requirements
t = Target(id='<공고id>', company='테스트회사', title='MLOps', src='wanted', url='')
print(fetch_requirements(t)[:200])"
```
Expected: 테스트 통과 + FastAPI가 포함된 요건 텍스트 출력(마감됐으면 다른 id로 — `candidates.json`의 rows에서 마감 null인 행을 고른다).

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "Phase 2: load_targets + fetch_requirements (미점수 선정·요건 수집)"
```

---

### Task 4: 루브릭 프롬프트 + judge activity (llm)

**Files:**
- Create: `prompts/rubric_v1.md`, `jobscouter/judge.py`, `.env`(키는 사용자 기입)
- Modify: `pyproject.toml` (anthropic, python-dotenv 추가), `jobscouter/config.py` (JudgeInput/Judgment/모델 상수), `jobscouter/worker.py` (llm 서브커맨드)
- Test: `tests/test_judge.py`

**Interfaces:**
- Consumes: `config.Target`
- Produces:
  - `config.JudgeInput`: `target: Target`, `requirements: str`, `search_context: str = ""`, `max_tokens: int = 2000`
  - `config.Judgment`: `id/company/title: str`, `scores: list[int]`(5원소 `[스택,도메인,레벨,역할,감점]`), `total: int`, `exclude: bool`, `reason: str`, `quotes: list[str]`, `confidence: float`, `rubric_version: str`, `usage: dict`(캐시 적중 시 `{}`), `cached: bool`
  - `judge.judge(inp: JudgeInput) -> Judgment` (activity, llm 큐) — 캐시 `data/judgments.jsonl`
  - `judge.RUBRIC_VERSION = "v1"`, `judge.factbase_hash() -> str`
  - worker CLI에 `llm` 서브커맨드 (.env 로드, Q_LLM 전용, `max_task_queue_activities_per_second=0.5`)

- [ ] **Step 1: 의존성 추가**

```bash
uv add anthropic python-dotenv
```

- [ ] **Step 2: prompts/rubric_v1.md**

SKILL.md §5~6을 판정 전용으로 이식한다. **배점표·감점 5항목·판정 함정(제목≠필수요건, 우대≠필수, 데이터모델링≠모델학습)을 그대로 옮긴다** — 아래 뼈대에 SKILL.md §5~6 원문 표를 채워 넣는 것이 구현 작업이다:

````markdown
# 공고 판정 루브릭 v1

너는 <이름>의 채용 공고 판정기다. 아래 사실베이스에 근거해 공고 1건을
100점 루브릭으로 채점한다. 반드시 score_job 도구로만 답한다.

## 채점 축 (상한: 스택35 / 도메인25 / 레벨20 / 역할20 / 감점 -25~0)

[SKILL.md §6의 배점표 원문]

## 핵심 규칙

- 스택 35의 조건은 "제목"이 아니라 **자격요건 필수 블록**이다. 우대사항에만
  있으면 35가 아니다. (2026-08-11 감사: 35점 31건 중 14건이 이 오류)
- 감점은 자격요건 **필수**에만 적용. 우대는 감점 0. 항목당 -5, 상한 -25.
- 감점 5항목(전부 본인 확인 완료): <미보유 기술 5개 — prompts/rubric_v1.example.md의
  「감점 축」 표를 채워 넣는다>
- **핵심 업무가 통째로 미보유면 감점이 아니라 제외**(exclude=true). 필수요건
  여러 줄 중 한 줄일 때만 -5.
- 키워드 표면 매칭 금지: "데이터 모델링"은 모델 학습이 아니다. "대용량 문서
  처리"는 대규모 트래픽이 아니다. 문장을 읽고 판정한다.
- 자격요건에서 먼저 확인: ① 언어 나열 순서(무엇이 먼저인가) ② 모델 학습 표현
  ③ 프레임워크가 필수인가 우대인가 ④ 감점 5항목이 필수 블록 안에 있는가
- 타 스택이 주력이면 스택 8. 직무 불일치(프론트·QA·펌웨어 등)나 레벨 극단
  불일치는 exclude.
- quotes에는 판정 근거가 된 자격요건 원문 문장을 그대로 인용한다.
- confidence: 요건이 명확하면 0.9+, 자격요건에 기술 언급이 거의 없거나
  해석이 갈리면 0.6 이하.

## 사실베이스

{factbase}
````

`{factbase}`는 judge가 로드 시 치환. 검색 컨텍스트(Phase 3)는 user 메시지에 붙는다.

- [ ] **Step 3: config.py에 JudgeInput/Judgment 추가**

```python
JUDGE_MODEL = os.environ.get("JOBSCOUTER_MODEL", "claude-sonnet-5")


@dataclass
class JudgeInput:
    target: Target
    requirements: str
    search_context: str = ""   # Phase 3: 판례·평판·사실 발췌
    max_tokens: int = 2000     # 폭주 방지 — 타입으로 강제


@dataclass
class Judgment:
    id: str
    company: str
    title: str
    scores: list[int]          # [스택,도메인,레벨,역할,감점(-25~0)]
    total: int
    exclude: bool
    reason: str
    quotes: list[str] = field(default_factory=list)
    confidence: float = 0.0
    rubric_version: str = ""
    usage: dict = field(default_factory=dict)   # {in,out,cache_read,model,ms}
    cached: bool = False
```

- [ ] **Step 4: judge.py**

```python
"""llm 큐 activity — ANTHROPIC_API_KEY는 이 모듈을 로드하는 워커 프로세스에만 있다."""
import hashlib
import json
import time

from temporalio import activity

from jobscouter.config import (DATA, FACTBASE, JUDGE_MODEL, PROMPTS,
                               JudgeInput, Judgment)

RUBRIC_VERSION = "v1"
_CAPS = [35, 25, 20, 20]
_CACHE = DATA / "judgments.jsonl"

SCORE_TOOL = {
    "name": "score_job",
    "description": "공고 1건의 루브릭 채점 결과",
    "input_schema": {
        "type": "object",
        "properties": {
            "scores": {"type": "array", "items": {"type": "integer"},
                       "minItems": 5, "maxItems": 5,
                       "description": "[스택0-35, 도메인0-25, 레벨0-20, 역할0-20, 감점-25~0]"},
            "exclude": {"type": "boolean",
                        "description": "핵심 업무 통째 미보유·직무 불일치 등 등재 불가"},
            "reason": {"type": "string", "description": "판정 사유 3문장 이내"},
            "quotes": {"type": "array", "items": {"type": "string"},
                       "description": "근거가 된 자격요건 원문 인용"},
            "confidence": {"type": "number", "minimum": 0, "maximum": 1},
        },
        "required": ["scores", "exclude", "reason", "quotes", "confidence"],
    },
}


def factbase_hash() -> str:
    return hashlib.sha256(FACTBASE.read_bytes()).hexdigest()[:12]


def _cache_key(inp: JudgeInput, fb_hash: str) -> str:
    return f"{inp.target.id}|{RUBRIC_VERSION}|{fb_hash}"


def _load_cache() -> dict[str, dict]:
    if not _CACHE.exists():
        return {}
    return {r["key"]: r for r in map(json.loads, _CACHE.read_text().splitlines())}


def _validate(s: list[int]) -> list[int]:
    if len(s) != 5:
        raise ValueError(f"점수 {len(s)}원소 (5원소여야 함)")
    for i, (v, m) in enumerate(zip(s, _CAPS)):
        if not 0 <= v <= m:
            raise ValueError(f"{['스택','도메인','레벨','역할'][i]} {v} (상한 {m})")
    if not -25 <= s[4] <= 0:
        raise ValueError(f"감점 {s[4]} (범위 -25~0)")
    return s


@activity.defn
def judge(inp: JudgeInput) -> Judgment:
    fb = factbase_hash()
    key = _cache_key(inp, fb)
    hit = _load_cache().get(key)
    if hit:
        return Judgment(**hit["judgment"], cached=True)

    import anthropic  # llm 워커에서만 로드
    client = anthropic.Anthropic()
    rubric = (PROMPTS / f"rubric_{RUBRIC_VERSION}.md").read_text()
    system = [{"type": "text",
               "text": rubric.replace("{factbase}", FACTBASE.read_text()),
               "cache_control": {"type": "ephemeral"}}]  # 사이클 내 불변 → 캐시 적중
    user = (f"회사: {inp.target.company}\n포지션: {inp.target.title}\n"
            f"출처: {inp.target.src} {inp.target.url}\n\n"
            f"자격요건 원문:\n{inp.requirements}")
    if inp.search_context:
        user += f"\n\n<검색 컨텍스트>\n{inp.search_context}\n</검색 컨텍스트>"

    t0 = time.monotonic()
    r = client.messages.create(
        model=JUDGE_MODEL, max_tokens=inp.max_tokens, system=system,
        messages=[{"role": "user", "content": user}],
        tools=[SCORE_TOOL], tool_choice={"type": "tool", "name": "score_job"})
    out = next(b.input for b in r.content if b.type == "tool_use")
    scores = _validate([int(x) for x in out["scores"]])

    j = Judgment(
        id=inp.target.id, company=inp.target.company, title=inp.target.title,
        scores=scores, total=sum(scores), exclude=bool(out["exclude"]),
        reason=out["reason"], quotes=list(out["quotes"]),
        confidence=float(out["confidence"]), rubric_version=RUBRIC_VERSION,
        usage={"in": r.usage.input_tokens, "out": r.usage.output_tokens,
               "cache_read": getattr(r.usage, "cache_read_input_tokens", 0) or 0,
               "model": JUDGE_MODEL,
               "ms": int((time.monotonic() - t0) * 1000)})
    DATA.mkdir(exist_ok=True)
    rec = {"key": key, "judgment": {k: v for k, v in j.__dict__.items()
                                    if k != "cached"}}
    with _CACHE.open("a") as f:
        f.write(json.dumps(rec, ensure_ascii=False) + "\n")
    return j
```

- [ ] **Step 5: worker.py에 llm 서브커맨드 추가**

`main()`의 if-체인에 추가:
```python
    elif cmd == "llm":
        from dotenv import load_dotenv
        load_dotenv()  # ANTHROPIC_API_KEY — 이 프로세스에만
        import os
        if not os.environ.get("ANTHROPIC_API_KEY"):
            sys.exit(".env에 ANTHROPIC_API_KEY가 없다")
        from jobscouter import judge as judge_mod
        from jobscouter.config import Q_LLM
        worker = Worker(
            client, task_queue=Q_LLM, activities=[judge_mod.judge],
            activity_executor=ThreadPoolExecutor(2),
            max_task_queue_activities_per_second=0.5)  # 레이트리밋은 큐 레벨
        print(f"llm 워커 시작 — {TEMPORAL} / {Q_LLM}")
        await worker.run()
```

- [ ] **Step 6: 테스트 — 검증·캐시·자격증명 격리**

`tests/test_judge.py`:
```python
import sys

import pytest

import jobscouter.io_acts  # noqa: F401 — 격리 검사를 위해 먼저 로드
import jobscouter.workflow  # noqa: F401
from jobscouter import judge as J
from jobscouter.config import JudgeInput, Judgment, Target


def test_io_modules_never_import_anthropic():
    """자격증명 격리 — io·workflow 모듈 로드가 anthropic을 끌고 오면 실패."""
    assert "anthropic" not in sys.modules


def test_validate_caps():
    assert J._validate([35, 25, 20, 20, -25]) == [35, 25, 20, 20, -25]
    with pytest.raises(ValueError):
        J._validate([36, 0, 0, 0, 0])       # 스택 상한 초과
    with pytest.raises(ValueError):
        J._validate([30, 10, 16, 20, 5])    # 감점이 양수
    with pytest.raises(ValueError):
        J._validate([30, 10, 16, 20])       # 4원소


def test_cache_roundtrip(tmp_path, monkeypatch):
    monkeypatch.setattr(J, "_CACHE", tmp_path / "j.jsonl")
    monkeypatch.setattr(J, "DATA", tmp_path)
    monkeypatch.setattr(J, "factbase_hash", lambda: "abc123")
    called = []

    class FakeUsage:
        input_tokens, output_tokens, cache_read_input_tokens = 100, 50, 0

    class FakeMsg:
        usage = FakeUsage()
        content = [type("B", (), {"type": "tool_use", "input": {
            "scores": [30, 18, 20, 16, 0], "exclude": False,
            "reason": "필수에 Python", "quotes": ["Python 경험"],
            "confidence": 0.9}})()]

    class FakeClient:
        class messages:
            @staticmethod
            def create(**kw):
                called.append(kw)
                return FakeMsg()

    monkeypatch.setitem(sys.modules, "anthropic",
                        type("M", (), {"Anthropic": lambda: FakeClient()}))
    inp = JudgeInput(target=Target(id="1", company="c", title="t",
                                   src="wanted", url="u"),
                     requirements="Python 경험")
    j1 = J.judge(inp)
    j2 = J.judge(inp)
    assert (j1.cached, j2.cached) == (False, True)
    assert len(called) == 1              # 두 번째는 API 안 감
    assert j2.total == 84 and j2.rubric_version == "v1"
```

- [ ] **Step 7: 실행 + 라이브 스모크 1건**

```bash
uv run pytest -q
echo 'ANTHROPIC_API_KEY=' > .env   # 사용자에게 키 기입 요청 — BLOCKING
```
사용자가 키를 채운 뒤:
```bash
uv run python -c "
from dotenv import load_dotenv; load_dotenv()
from jobscouter.config import JudgeInput, Target
from jobscouter.judge import judge
t = Target(id='<공고id>', company='테스트회사', title='MLOps 엔지니어', src='wanted', url='')
from jobscouter.io_acts import fetch_requirements
j = judge(JudgeInput(target=t, requirements=fetch_requirements(t)))
print(j.scores, j.total, j.exclude, j.confidence)
print(j.reason)
print(j.usage)"
```
Expected: 수동 판정 `[35, 15, 20, 18]`(88점)과 근사한 점수, usage에 토큰·ms 기록. (공고가 내려갔으면 rows에서 살아있는 행으로 대체.)

- [ ] **Step 8: Commit**

```bash
git add -A && git commit -m "Phase 2: 루브릭 v1 + judge activity (forced-tool, 캐시, usage)"
```

skipped: agent loop(max_turns)는 만들지 않음 — Phase 2~3의 judge는 단일 forced-tool 호출로 충분하고, 도구(RAG)도 입력 주입 방식이라 turn이 1이다. 도구 호출 loop가 생기는 시점에 max_turns 파라미터를 추가한다.

---

### Task 5: 워크플로에 judge 통합 + 예산 강등

**Files:**
- Modify: `jobscouter/config.py` (CycleParams 확장), `jobscouter/workflow.py`, `jobscouter/worker.py` (run 파라미터)
- Test: `tests/test_workflow.py` (judge·예산 케이스 추가)

**Interfaces:**
- Consumes: `io_acts.load_targets`, `io_acts.fetch_requirements`, `judge.judge`
- Produces:
  - `CycleParams` 추가 필드: `budget_tokens: int = 2_000_000`, `chunk: int = 8`, `max_tokens: int = 2000`
  - `JobScoutCycle` 내부 상태: `self._judged: list[Judgment]`, `self._demoted: list[str]`
  - `status()` 쿼리 확장: `{stage, browser, judged, excluded, demoted, spent_tokens, proposals: [{id,company,title,total,confidence}...]}`
  - 사이클 순서: fetch → load_targets → judge×N(예산 내) → 브라우저 signal 대기 → refresh → build (승인 게이트는 Task 9에서 삽입)

- [ ] **Step 1: CycleParams 확장**

```python
@dataclass
class CycleParams:
    browser_wait_minutes: int = 120
    budget_tokens: int = 2_000_000   # 초과 시 잔여는 미점수 강등
    chunk: int = 8                   # 동시 judge 수 — 청크 사이에서 예산 체크
    max_tokens: int = 2000           # judge 1회 출력 상한
```

- [ ] **Step 2: workflow.py — fetch 이후·browser 대기 이전에 판정 단계 삽입**

```python
_LLM_OPTS = dict(
    task_queue=Q_LLM,
    start_to_close_timeout=timedelta(minutes=5),
    retry_policy=RetryPolicy(maximum_attempts=3),
)
```

`run()`의 fetch와 browser 대기 사이에:
```python
        # 판정 대상 = 미점수 전부 (신규 + 백로그)
        self._stage = "대상 선정"
        targets = await workflow.execute_activity("load_targets", **_IO_OPTS)

        self._stage = f"판정 0/{len(targets)}"
        spent = 0
        for i in range(0, len(targets), params.chunk):
            if spent >= params.budget_tokens:
                # 우아한 강등 — 판정 없이 미점수로 남긴다 (build.py가 집계·보고)
                self._demoted = [t["id"] for t in targets[i:]]
                break
            batch = targets[i:i + params.chunk]

            async def _one(t):
                req = await workflow.execute_activity(
                    "fetch_requirements", t, **_IO_OPTS)
                return await workflow.execute_activity(
                    "judge",
                    {"target": t, "requirements": req,
                     "search_context": "", "max_tokens": params.max_tokens},
                    **_LLM_OPTS)

            results = await asyncio.gather(
                *(_one(t) for t in batch), return_exceptions=True)
            for t, r in zip(batch, results):
                if isinstance(r, BaseException):
                    self._failed.append(t["id"])   # 재시도 소진 — 미점수로 남음
                else:
                    self._judged.append(r)
                    spent += r["usage"].get("in", 0) + r["usage"].get("out", 0)
            self._stage = f"판정 {min(i + params.chunk, len(targets))}/{len(targets)}"
        self._spent = spent
        out["judge"] = {"judged": len(self._judged), "demoted": len(self._demoted),
                        "failed": len(self._failed), "spent_tokens": spent}
```

`__init__`에 `self._judged: list[dict] = []`, `self._demoted: list[str] = []`, `self._failed: list[str] = []`, `self._spent = 0` 추가. `import asyncio`를 파일 상단에.

주: dataclass activity 결과는 workflow 안에서 dict로 다뤄진다(문자열 이름 호출은 타입 힌트가 없어 payload가 dict로 온다). 축 인덱스 대신 키 접근만 쓴다.

`status()` 확장:
```python
    @workflow.query
    def status(self) -> dict:
        prop = [{"id": j["id"], "company": j["company"], "title": j["title"],
                 "total": j["total"], "confidence": j["confidence"]}
                for j in self._judged if not j["exclude"]]
        return {"stage": self._stage, "browser": self._browser_note,
                "judged": len(self._judged),
                "excluded": sum(1 for j in self._judged if j["exclude"]),
                "demoted": self._demoted, "failed": self._failed,
                "spent_tokens": self._spent,
                "proposals": sorted(prop, key=lambda p: -p["total"])}
```

- [ ] **Step 3: worker.py — io 워커에 신규 activity 등록, run에 옵션**

`io` 분기의 `acts`를:
```python
        acts = [io_acts.run_script, io_acts.load_targets, io_acts.fetch_requirements]
```
`run` 분기를:
```python
    elif cmd == "run":
        import argparse
        p = argparse.ArgumentParser()
        p.add_argument("--budget", type=int, default=2_000_000)
        p.add_argument("--browser-wait", type=int, default=120)
        a = p.parse_args(sys.argv[2:])
        from jobscouter.workflow import JobScoutCycle
        handle = await client.start_workflow(
            JobScoutCycle.run,
            CycleParams(budget_tokens=a.budget, browser_wait_minutes=a.browser_wait),
            id=WORKFLOW_ID, task_queue=Q_WF)
        print(f"시작: {handle.id} → http://{TEMPORAL.split(':')[0]}:8233")
```

- [ ] **Step 4: 테스트 — 판정 경로·예산 강등**

`tests/test_workflow.py`에 스텁·케이스 추가:
```python
@activity.defn(name="load_targets")
async def fake_load_targets() -> list[dict]:
    return [{"id": f"t{i}", "company": f"c{i}", "title": "포지션",
             "src": "wanted", "url": "u"} for i in range(4)]


@activity.defn(name="fetch_requirements")
async def fake_fetch_requirements(t: dict) -> str:
    return "필수: Python"


@activity.defn(name="judge")
async def fake_judge(inp: dict) -> dict:
    i = int(inp["target"]["id"][1:])
    return {"id": inp["target"]["id"], "company": inp["target"]["company"],
            "title": "포지션", "scores": [30, 18, 20, 16, 0], "total": 84,
            "exclude": i % 2 == 1, "reason": "r", "quotes": [],
            "confidence": 0.9, "rubric_version": "v1",
            "usage": {"in": 60_000, "out": 1_000}, "cached": False}
```

`_run_cycle`의 Worker 등록에 세 스텁 추가, 그리고:
```python
@pytest.mark.asyncio
async def test_budget_demotion():
    """chunk=2, 예산 100k, judge당 61k → 1청크(2건) 판정 후 잔여 2건 강등."""
    env = await WorkflowEnvironment.start_time_skipping()
    try:
        q = f"test-{uuid.uuid4()}"
        async with Worker(env.client, task_queue=q, workflows=[JobScoutCycle],
                          activities=[fake_run_script, fake_load_targets,
                                      fake_fetch_requirements, fake_judge]):
            import jobscouter.workflow as wf
            wf._IO_OPTS["task_queue"] = q
            wf._LLM_OPTS["task_queue"] = q
            handle = await env.client.start_workflow(
                JobScoutCycle.run,
                CycleParams(browser_wait_minutes=1, budget_tokens=100_000, chunk=2),
                id=f"wf-{uuid.uuid4()}", task_queue=q)
            await handle.signal(JobScoutCycle.browser_done, "ok")
            out = await handle.result()
            assert out["judge"]["judged"] == 2
            assert out["judge"]["demoted"] == 2   # 예산 초과 → 미점수 강등
            st = ...  # result 후 query 불가 — 강등 검증은 out으로 충분
    finally:
        await env.shutdown()
```

- [ ] **Step 5: 실행**

```bash
uv run pytest -q
```
Expected: 전부 통과.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "Phase 2: 워크플로 judge 통합 + 사이클 예산 강등"
```

---

### Task 6: eval — 수동 판정 대조 (Phase 2 완료 게이트)

**Files:**
- Create: `scripts/eval_judge.py`

**Interfaces:**
- Consumes: `judge.judge`(직접 함수 호출 — Temporal 불요), `io_acts.fetch_requirements`, `candidates.json`
- Produces: `data/eval_{날짜}.md` 대조표. **Phase 2 완료 판정 = 사용자가 이 표를 보고 일치율 승인.**

- [ ] **Step 1: scripts/eval_judge.py**

```python
"""수동 판정 대조 — rows에서 마감 안 된 최근 n건 + skipped s건을 재판정해 비교.

    uv run python scripts/eval_judge.py --n 15 --skipped 5 [--rag]
"""
import argparse
import json
import sys
from datetime import date
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from dotenv import load_dotenv

load_dotenv()
from jobscouter.config import DATA, JOBFEED, JudgeInput, Target
from jobscouter.io_acts import fetch_requirements
from jobscouter.judge import judge

AXES = ["스택", "도메인", "레벨", "역할", "감점"]


def _target(cid, company, title):
    cid = str(cid)
    src = "jumpit" if cid.startswith("j") else "wanted"
    url = (f"https://jumpit.saramin.co.kr/position/{cid[1:]}" if src == "jumpit"
           else f"https://www.wanted.co.kr/wd/{cid}")
    return Target(id=cid, company=company, title=title, src=src, url=url)


def main():
    p = argparse.ArgumentParser()
    p.add_argument("--n", type=int, default=15)
    p.add_argument("--skipped", type=int, default=5)
    p.add_argument("--rag", action="store_true")  # Phase 3에서 사용
    a = p.parse_args()

    cand = json.loads((JOBFEED / "candidates.json").read_text())
    rows = [r for r in cand["rows"] if r[7] != "closed"][-a.n:]
    skipped = list(cand["skipped"].items())[-a.skipped:]

    ctx = ""
    if a.rag:
        from jobscouter.search import search_context_text  # Task 8
    lines = [f"# eval {date.today()} — rag={'on' if a.rag else 'off'}",
             "", "| 회사 | 수동 | 자동 | Δ총점 | 축 일치 | conf |", "|---|---|---|---:|---|---|"]
    diffs, axis_hits, excl_ok = [], 0, 0

    for r in rows:
        t = _target(r[2], r[1], r[0])
        try:
            req = fetch_requirements(t)
        except Exception as e:
            lines.append(f"| {r[1]} | — | 요건 수집 실패: {e} | | | |")
            continue
        if a.rag:
            ctx = search_context_text(t, req)
        j = judge(JudgeInput(target=t, requirements=req, search_context=ctx))
        manual = r[3] + [0] * (5 - len(r[3]))
        match = sum(int(m == g) for m, g in zip(manual, j.scores))
        axis_hits += match
        d = j.total - sum(manual)
        diffs.append(abs(d))
        lines.append(f"| {r[1]} | {manual} {sum(manual)} | {j.scores} {j.total} "
                     f"| {d:+d} | {match}/5 | {j.confidence:.2f} |")

    lines += ["", "## skipped 대조 (exclude 일치 기대)", "",
              "| 회사 | 수동 사유 | 자동 exclude | 자동 사유 |", "|---|---|---|---|"]
    for cid, (company, title, why) in skipped:
        t = _target(cid, company, title)
        try:
            req = fetch_requirements(t)
        except Exception as e:
            lines.append(f"| {company} | {why[:40]} | 요건 수집 실패 | {e} |")
            continue
        if a.rag:
            ctx = search_context_text(t, req)
        j = judge(JudgeInput(target=t, requirements=req, search_context=ctx))
        excl_ok += int(j.exclude or j.total < 70)
        lines.append(f"| {company} | {why[:40]} | {j.exclude} ({j.total}점) "
                     f"| {j.reason[:60]} |")

    n = len(diffs) or 1
    lines += ["", f"**총점 MAE {sum(diffs)/n:.1f} · 축 일치 {axis_hits}/{n*5} · "
              f"skipped exclude 일치 {excl_ok}/{len(skipped)}**"]
    out = DATA / f"eval_{date.today()}{'_rag' if a.rag else ''}.md"
    out.write_text("\n".join(lines))
    print("\n".join(lines[-3:]))
    print("→", out)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행 (라이브 LLM — 20건, 사이클 예산 대비 소액)**

```bash
uv run python scripts/eval_judge.py --n 15 --skipped 5
```
Expected: `data/eval_2026-08-XX.md` 생성. 마감·API 실패 행은 표에 사유로 남는다.

- [ ] **Step 3: 사용자 게이트 — BLOCKING**

대조표를 사용자에게 요약 보고(MAE·축 일치율·불일치 상위 3건의 사유). **DESIGN Phase 2 완료 판정 = 사용자가 일치율을 보고 진행 승인.** 불일치 패턴이 루브릭 프롬프트 결함이면 rubric_v1.md를 수정하고 재실행(캐시키가 루브릭 버전 기준이므로 v1 수정 중에는 `data/judgments.jsonl` 삭제 후 재실행).

- [ ] **Step 4: Commit**

```bash
git add scripts && git commit -m "Phase 2: eval_judge — 수동 판정 대조 게이트"
```

---

# Phase 3 — ES 인덱스 + 검색 주입 (RAG 유무 품질 차이 측정)

### Task 7: ES LAN 개방(사용자) + jobscout 인덱스 3종 색인

**Files:**
- Create: `scripts/index_es.py`, `jobscouter/search.py`
- Modify: `deploy/MINI_SETUP.md` (§2 ES 절 추가), `pyproject.toml`, `jobscouter/config.py` (ES_URL)

**Interfaces:**
- Consumes: 서버 ES(:9200), `사실베이스`, `candidates.json`, `기업평판.md`
- Produces:
  - `config.ES_URL = os.environ.get("JOBSCOUTER_ES", "http://<server-ip>:9200")`
  - ES 인덱스: `jobscout_facts`(사실베이스 청크), `jobscout_precedents`(rows 161 + skipped 549), `jobscout_reputation`(기업평판 행) — 매핑 공통 `{content: text, company: keyword, kind: keyword, embedding: dense_vector 1024 cosine}`
  - `search.embed(texts: list[str]) -> list[list[float]]` (bge-m3 lazy 싱글턴)
  - `search.hybrid(index: str, query: str, k: int = 20, company: str | None = None) -> list[dict]` — BM25+kNN 각각 검색 후 RRF(1/(60+rank)) 융합
  - `scripts/index_es.py` — 인덱스 재생성 + 색인 (멱등: 지우고 다시)

- [ ] **Step 1: MINI_SETUP.md에 §2 추가 (사용자 실행)**

````markdown
## 2. ES LAN 개방 (Phase 3)

PKB ES는 127.0.0.1에만 바인드돼 있어 작업 머신 워커가 못 붙는다. LAN에도 연다
(집 LAN 단일 사용자 — temporal --ip 0.0.0.0과 같은 노출 수준).

서버에서:
```bash
cp ~/opt/elasticsearch/config/elasticsearch.yml ~/opt/elasticsearch/config/elasticsearch.yml.bak
# elasticsearch.yml 수정:
#   network.host: ["127.0.0.1", "<server-ip>"]
#   discovery.type: single-node        ← 이미 있으면 그대로 (없으면 추가 — 비루프백
#                                        바인드 시 프로덕션 부트스트랩 검사를 우회)
launchctl kickstart -k gui/$(id -u)/dev.<user>.pkb-es
sleep 20 && curl -s localhost:9200/_cluster/health | head -3   # status green/yellow
```

작업 머신에서 검증:
```bash
curl -s http://<server-ip>:9200 | head -5   # cluster_name: pkb
```

롤백: `.bak` 복원 후 kickstart. PKB 서비스는 localhost로 붙으므로 영향 없음.
````

- [ ] **Step 2: 사용자에게 §2 실행 요청 — BLOCKING.** 완료 후 작업 머신에서 `curl -s http://<server-ip>:9200` 확인.

- [ ] **Step 3: 의존성 + config**

```bash
uv add "elasticsearch>=8.17,<9" sentence-transformers
```
config.py에:
```python
ES_URL = os.environ.get("JOBSCOUTER_ES", "http://<server-ip>:9200")
```

- [ ] **Step 4: jobscouter/search.py**

```python
"""ES 색인·검색 — io 큐 소속(자격증명 없음). 임베딩은 bge-m3 로컬(PKB 채택 모델)."""
import threading

from elasticsearch import Elasticsearch

from jobscouter.config import ES_URL

EMBED_MODEL = "BAAI/bge-m3"   # PKB 2026-07 매트릭스 실측 채택 — 1024d cosine
_model = None
_lock = threading.Lock()
INDICES = ("jobscout_facts", "jobscout_precedents", "jobscout_reputation")
MAPPING = {"properties": {
    "content": {"type": "text"},
    "company": {"type": "keyword"},
    "kind": {"type": "keyword"},   # fact | listed | skipped | reputation
    "embedding": {"type": "dense_vector", "dims": 1024,
                  "index": True, "similarity": "cosine"},
}}


def es() -> Elasticsearch:
    return Elasticsearch(ES_URL, request_timeout=30)


def embed(texts: list[str]) -> list[list[float]]:
    global _model
    with _lock:
        if _model is None:
            from sentence_transformers import SentenceTransformer
            _model = SentenceTransformer(EMBED_MODEL)
    return [v.tolist() for v in _model.encode(texts, show_progress_bar=False)]


def hybrid(index: str, query: str, k: int = 20,
           company: str | None = None) -> list[dict]:
    """BM25·kNN 각각 → RRF(1/(60+rank)) 융합 — PKB retrieve.py 패턴, 리랭커 없음."""
    client = es()
    filt = [{"term": {"company": company}}] if company else []
    bm25 = client.search(index=index, size=k, query={
        "bool": {"must": {"match": {"content": query}}, "filter": filt}})
    knn = client.search(index=index, size=k, knn={
        "field": "embedding", "query_vector": embed([query])[0],
        "k": k, "num_candidates": k * 5, "filter": filt})
    scores: dict[str, float] = {}
    docs: dict[str, dict] = {}
    for res in (bm25, knn):
        for rank, h in enumerate(res["hits"]["hits"]):
            scores[h["_id"]] = scores.get(h["_id"], 0) + 1 / (60 + rank)
            docs[h["_id"]] = {k2: v for k2, v in h["_source"].items()
                              if k2 != "embedding"}
    top = sorted(scores, key=scores.get, reverse=True)[:k]
    return [docs[i] for i in top]
```

- [ ] **Step 5: scripts/index_es.py**

```python
"""jobscout 인덱스 3종 재생성 + 색인. 멱등 — 매 실행 지우고 다시.

    uv run python scripts/index_es.py
"""
import json
import re
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from elasticsearch.helpers import bulk

from jobscouter.config import FACTBASE, JOBFEED
from jobscouter.search import INDICES, MAPPING, embed, es


def fact_docs():
    """사실베이스를 ## 절 단위로 청크. 2000자 넘는 절은 문단으로 재분할."""
    text = FACTBASE.read_text()
    for sec in re.split(r"\n(?=## )", text):
        sec = sec.strip()
        if not sec:
            continue
        chunks = ([sec] if len(sec) <= 2000 else
                  [p for p in sec.split("\n\n") if p.strip()])
        for c in chunks:
            yield {"content": c[:2000], "company": "", "kind": "fact"}


def precedent_docs():
    cand = json.loads((JOBFEED / "candidates.json").read_text())
    for r in cand["rows"]:
        scores = r[3] + [0] * (5 - len(r[3]))
        rep = f" 평판: {r[4][4]}" if r[4] else (f" 평판없음: {r[5]}" if r[5] else "")
        yield {"company": r[1], "kind": "listed",
               "content": f"[등재 {sum(scores)}점 {scores}] {r[1]} | {r[0]}{rep}"}
    for cid, (company, title, why) in cand["skipped"].items():
        yield {"company": company, "kind": "skipped",
               "content": f"[제외] {company} | {title} | 사유: {why}"}


def reputation_docs():
    """기업평판.md의 표 행 단위 — 첫 셀을 회사명으로 본다."""
    rep = JOBFEED / "기업평판.md"
    if not rep.exists():
        return
    for ln in rep.read_text().splitlines():
        cells = [c.strip() for c in ln.split("|")]
        if ln.startswith("|") and len(cells) > 3 and cells[1] and \
                not set(cells[1]) <= {"-", ":", " "}:
            yield {"company": cells[1], "kind": "reputation",
                   "content": ln[:1500]}


def main():
    client = es()
    sources = dict(zip(INDICES, (fact_docs, precedent_docs, reputation_docs)))
    for index, source in sources.items():
        if client.indices.exists(index=index):
            client.indices.delete(index=index)
        client.indices.create(index=index, mappings=MAPPING)
        docs = list(source())
        vecs = embed([d["content"] for d in docs])
        bulk(client, ({"_index": index, **d, "embedding": v}
                      for d, v in zip(docs, vecs)))
        print(f"{index}: {len(docs)}건")


if __name__ == "__main__":
    main()
```

- [ ] **Step 6: 색인 실행 + 검색 스모크**

```bash
uv run python scripts/index_es.py
# Expected: jobscout_facts ~수십건 / jobscout_precedents ~710건 / jobscout_reputation ~수십건
uv run python -c "
from jobscouter.search import hybrid
for d in hybrid('jobscout_precedents', 'Django FastAPI 백엔드 필수', k=5):
    print(d['kind'], d['content'][:80])"
```
Expected: 스택 35 등재/제외 판례가 상위에 (등재된 판례 행).

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "Phase 3: jobscout ES 인덱스 3종 + bge-m3 하이브리드 검색(RRF)"
```

skipped: 증분 색인 — 판례 수백 건 전체 재색인이 1분 미만이라 멱등 재생성으로 충분. 판정 판례가 만 단위가 되면 증분으로.

---

### Task 8: judge에 검색 컨텍스트 주입 + RAG 효과 측정 (Phase 3 게이트)

**Files:**
- Modify: `jobscouter/search.py` (search_context activity), `jobscouter/workflow.py`, `jobscouter/worker.py` (io 워커에 등록), `scripts/eval_judge.py`는 Task 6에서 `--rag` 이미 준비됨

**Interfaces:**
- Consumes: `search.hybrid`, `judge.judge`(search_context 파라미터는 Task 4부터 존재)
- Produces:
  - `search.search_context_text(t: Target, requirements: str) -> str` (일반 함수 — eval용)
  - `search.search_context(t: Target, requirements: str) -> str` (activity 래퍼, io 큐)
  - 워크플로 `_one()`이 fetch_requirements → search_context → judge 순으로 연결

- [ ] **Step 1: search.py에 추가**

```python
from temporalio import activity

from jobscouter.config import Target


def search_context_text(t: Target, requirements: str) -> str:
    """판례·평판·사실 발췌를 judge 입력용 한 블록으로. 총 ~2500자 캡."""
    q = f"{t.company} {t.title} {requirements[:200]}"
    parts = []
    prec = hybrid("jobscout_precedents", q, k=8)
    if prec:
        parts.append("[판정 판례]\n" + "\n".join(d["content"][:200] for d in prec))
    rep = hybrid("jobscout_reputation", t.company, k=3, company=None)
    rep = [d for d in rep if d["company"] and d["company"] in t.company] or rep[:1]
    if rep:
        parts.append("[기업평판 캐시]\n" + "\n".join(d["content"][:250] for d in rep))
    facts = hybrid("jobscout_facts", requirements[:300], k=6)
    if facts:
        parts.append("[사실베이스 관련 절]\n" + "\n".join(d["content"][:200] for d in facts))
    return "\n\n".join(parts)[:2500]


@activity.defn
def search_context(t: Target, requirements: str) -> str:
    return search_context_text(t, requirements)
```

주: 사실베이스 전문이 이미 system에 있으므로 `[사실베이스 관련 절]`은 중복이지만, 관련 절을 user 쪽에 가깝게 재제시하는 것이 판정 인용을 유도한다 — eval에서 효과 없으면 뺀다.

- [ ] **Step 2: workflow.py `_one()` 수정**

```python
            async def _one(t):
                req = await workflow.execute_activity(
                    "fetch_requirements", t, **_IO_OPTS)
                ctx = await workflow.execute_activity(
                    "search_context", args=[t, req], **_IO_OPTS)
                return await workflow.execute_activity(
                    "judge",
                    {"target": t, "requirements": req,
                     "search_context": ctx, "max_tokens": params.max_tokens},
                    **_LLM_OPTS)
```

**캐시 주의**: search_context가 judge 입력에 들어가지만 캐시키는 `(공고id, 루브릭버전, 사실베이스해시)` 그대로다(DESIGN 결정). 판례가 늘어도 기존 판정을 무효화하지 않는다 — 재판정 트리거는 루브릭·사실베이스 변경뿐.

- [ ] **Step 3: worker.py io 분기에 등록**

```python
        from jobscouter import search
        acts = [io_acts.run_script, io_acts.load_targets,
                io_acts.fetch_requirements, search.search_context]
```
(io 워커가 sentence-transformers를 로드한다 — llm 큐가 아니라 io 큐인 이유는 자격증명이 아니라 ES·모델 IO이기 때문.)

- [ ] **Step 4: 워크플로 테스트 스텁 추가**

`tests/test_workflow.py`에 `fake_search_context` 스텁(`return "판례: ..."`)을 추가하고 Worker 등록 목록에 넣는다. 기존 두 테스트가 그대로 통과해야 한다.

```bash
uv run pytest -q
```

- [ ] **Step 5: RAG 유무 대조 (Phase 3 게이트) — BLOCKING**

```bash
rm -f data/judgments.jsonl        # rag-off 판정과 섞이지 않게 (eval 재판정 목적)
uv run python scripts/eval_judge.py --n 15 --skipped 5 --rag
```
`data/eval_*_rag.md`와 Task 6의 rag-off 결과를 비교해 사용자에게 보고:
- 총점 MAE·축 일치율 변화
- **판례 인용이 판정 사유(reason/quotes)에 등장하는가 — DESIGN Phase 3 완료 판정**
- 사용자 승인 후 다음 task로.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "Phase 3: 검색 컨텍스트 주입 + RAG 유무 대조"
```

---

# Phase 4 — 승인 signal → 커밋 자동화 + report + 주 1회 스케줄

### Task 9: 승인 게이트 + candidates.json 커밋 (io)

**Files:**
- Modify: `jobscouter/io_acts.py` (commit_rows), `jobscouter/workflow.py` (approve signal), `jobscouter/worker.py` (approve 서브커맨드), `jobscouter/config.py` (CycleParams.approve_wait_hours, dry_run)
- Test: `tests/test_io_acts.py`, `tests/test_workflow.py`

**Interfaces:**
- Consumes: `Judgment` dict (workflow 상태)
- Produces:
  - `io_acts.to_row(j: dict) -> list` — Judgment → candidates.json 8필드 행 (순수 함수)
  - `io_acts.commit_rows(approved: list[dict], dry_run: bool) -> str` (activity) — 행 추가 + 루브릭 버전 균일 검사 + git commit(${DATA_REPO} repo, candidates.json만)
  - workflow signal `approve(ids: list[str])` — 빈 리스트 = 등재 없음
  - CLI `approve id1 id2 ...` / `approve --none`
  - 사이클 순서 확정: fetch → judge×N → 브라우저 signal → **approve signal** → commit_rows → refresh → build → report(Task 10)

- [ ] **Step 1: io_acts.py에 추가**

```python
import subprocess as sp


def to_row(j: dict) -> list:
    """Judgment → candidates.json 8필드 행. 마감·근무지는 refresh_due.py가 채운다.
    평판 배열 자동 기입은 범위 외 — rep=null + 사유로 두고 사람이 보강한다."""
    cid = j["id"]
    rid = int(cid) if not cid.startswith("j") else cid   # 기존 관례: 원티드 int
    reason = (f"자동판정 rubric {j['rubric_version']} (conf {j['confidence']:.2f}) — "
              f"{j['reason']}")[:200]
    return [j["title"], j["company"], rid, j["scores"], None, reason, [], None]


@activity.defn
def commit_rows(approved: list[dict], dry_run: bool = False) -> str:
    if not approved:
        return "등재 0건"
    vers = {j["rubric_version"] for j in approved}
    if len(vers) != 1:
        # 루브릭이 섞인 배치 거부 — build.py의 배점 검사와 같은 층 (DESIGN)
        raise RuntimeError(f"루브릭 버전 혼재 {vers} — 배치 거부")
    path = JOBFEED / "candidates.json"
    cand = json.loads(path.read_text())
    have = {str(r[2]) for r in cand["rows"]}
    rows = [to_row(j) for j in approved if str(j["id"]) not in have]
    if dry_run:
        return f"dry-run: {len(rows)}건\n" + "\n".join(
            json.dumps(r, ensure_ascii=False) for r in rows)
    cand["rows"].extend(rows)
    path.write_text(json.dumps(cand, ensure_ascii=False, indent=1))
    repo = str(JOBFEED.parent)
    sp.run(["git", "-C", repo, "add", "jobfeed/candidates.json"], check=True)
    r = sp.run(["git", "-C", repo, "commit", "-m",
                f"job-scouter: 자동 등재 {len(rows)}건 (rubric {vers.pop()})"],
               capture_output=True, text=True)
    return f"등재 {len(rows)}건 · git: {'커밋됨' if r.returncode == 0 else r.stdout + r.stderr}"
```

- [ ] **Step 2: config.py CycleParams에 추가**

```python
    approve_wait_hours: int = 48
    dry_run: bool = False        # commit_rows까지 dry-run — 리허설용
```

- [ ] **Step 3: workflow.py — browser 대기와 refresh 사이에 승인 게이트**

`__init__`에 `self._approved: list[str] | None = None` 추가, signal 추가:
```python
    @workflow.signal
    def approve(self, ids: list[str]) -> None:
        self._approved = list(ids)
```

browser 대기 블록과 refresh 사이에:
```python
        # 등재 후보 배치 승인 — 사람이 signal로 확정해야 candidates.json에 커밋 (DESIGN)
        self._stage = "승인 대기"
        try:
            await workflow.wait_condition(
                lambda: self._approved is not None,
                timeout=timedelta(hours=params.approve_wait_hours))
        except TimeoutError:
            self._approved = []   # 타임아웃 = 등재 없음 — 판정 캐시는 남아 재사용
        chosen = [j for j in self._judged
                  if not j["exclude"] and j["id"] in set(self._approved)]
        self._stage = "커밋"
        out["commit"] = await workflow.execute_activity(
            "commit_rows", args=[chosen, params.dry_run], **_IO_OPTS)
```

- [ ] **Step 4: worker.py**

io 분기 `acts`에 `io_acts.commit_rows` 추가. 서브커맨드 추가:
```python
    elif cmd == "approve":
        ids = [] if sys.argv[2:] == ["--none"] else sys.argv[2:]
        await client.get_workflow_handle(WORKFLOW_ID).signal("approve", ids)
        print(f"승인 signal: {ids or '등재 없음'}")
```
`run` 분기 argparse에 `--dry-run` 플래그 추가 → `CycleParams(dry_run=a.dry_run, ...)`.

- [ ] **Step 5: 테스트**

`tests/test_io_acts.py`:
```python
def test_to_row_format():
    j = {"id": "j555", "company": "새회사", "title": "백엔드",
         "scores": [30, 18, 20, 16, -5], "total": 79, "exclude": False,
         "reason": "필수에 Python", "confidence": 0.9, "rubric_version": "v1"}
    r = io_acts.to_row(j)
    assert len(r) == 8 and r[2] == "j555" and r[4] is None and r[5]
    j["id"] = "365172"
    assert io_acts.to_row(j)[2] == 365172   # 원티드는 int — 기존 관례

def test_commit_rows_rejects_mixed_rubric():
    js = [{"id": "1", "rubric_version": "v1"}, {"id": "2", "rubric_version": "v2"}]
    import pytest
    with pytest.raises(RuntimeError, match="혼재"):
        io_acts.commit_rows(js, dry_run=True)
```

`tests/test_workflow.py`: `fake_commit_rows` 스텁(승인된 id 목록을 전역에 기록) 추가, `_run_cycle`류 헬퍼에서 `approve` signal 송신 케이스:
```python
@activity.defn(name="commit_rows")
async def fake_commit_rows(approved: list[dict], dry_run: bool) -> str:
    COMMITTED.extend(j["id"] for j in approved)
    return f"등재 {len(approved)}건"
```
케이스: judge 스텁이 t0·t2를 비제외로 반환 → `approve(["t0"])` → COMMITTED == ["t0"] (t2는 미승인·제외 안 됨) 검증. 기존 테스트에는 `await handle.signal(JobScoutCycle.approve, [])` 한 줄 추가(무승인 경로).

```bash
uv run pytest -q
```

- [ ] **Step 6: 리허설 (dry-run 사이클)**

```bash
uv run python -m jobscouter.worker io &
uv run python -m jobscouter.worker llm &
uv run python -m jobscouter.worker run --dry-run --budget 200000
uv run python -m jobscouter.worker status          # 판정 진행 관찰
uv run python -m jobscouter.worker browser-done "리허설"
uv run python -m jobscouter.worker status          # proposals 확인
uv run python -m jobscouter.worker approve --none  # 또는 상위 id 1개로 dry-run 커밋 확인
# 완료 후 UI 타임라인 확인, kill %1 %2
```
Expected: 실제 미점수 공고들이 판정되고(예산 20만 토큰 내) proposals에 정렬돼 나타남. dry-run이라 candidates.json 불변.

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "Phase 4: 승인 signal → candidates.json 커밋 (루브릭 혼재 거부)"
```

skipped: 평판 배열(rep) 자동 기입 — 기업평판.md 파싱→판정은 반어법 오탐 이력(2026-08-11)이 있는 사람 영역. rep=null+사유로 두고 사람이 보강. confidence 낮은 판정의 별도 검토 큐도 proposals의 confidence 노출로 대체(사람이 보고 승인 안 하면 그만).

---

### Task 10: report activity + 사이클 요약

**Files:**
- Modify: `jobscouter/judge.py` (report activity), `jobscouter/workflow.py` (마지막 단계), `jobscouter/worker.py` (llm 워커에 등록)
- Test: `tests/test_workflow.py` (스텁 추가)

**Interfaces:**
- Consumes: workflow 집계 dict
- Produces:
  - `judge.report(stats: dict) -> str` (activity, llm 큐) — `jobfeed/reports/{날짜}_자동사이클.md` 작성, 경로 반환 (수동 `매칭조사.md`와 파일명 구분)
  - workflow 반환값에 `report` 경로·`spent_tokens` 포함

- [ ] **Step 1: judge.py에 report 추가**

```python
@activity.defn
def report(stats: dict) -> str:
    """사이클 요약 md — 서술은 LLM, 수치는 stats 그대로."""
    import anthropic
    from datetime import date

    from jobscouter.config import JOBFEED
    client = anthropic.Anthropic()
    r = client.messages.create(
        model=JUDGE_MODEL, max_tokens=1500,
        messages=[{"role": "user", "content":
                   "다음 job-scouter 사이클 통계로 간결한 운영 보고서 md를 써라. "
                   "섹션: 요약(2문장) / 등재·제외 / 강등·실패 / 비용·latency. "
                   "수치를 지어내지 말 것:\n" + json.dumps(stats, ensure_ascii=False)}])
    body = r.content[0].text
    path = JOBFEED / "reports" / f"{date.today()}_자동사이클.md"
    path.write_text(f"# {date.today()} 자동 사이클\n\n{body}\n")
    return str(path)
```
(JOBFEED import는 파일 상단 config import에 합친다.)

- [ ] **Step 2: workflow.py — build 뒤 마지막 단계**

```python
        self._stage = "report"
        stats = {
            "targets": len(targets), "judged": len(self._judged),
            "excluded": sum(1 for j in self._judged if j["exclude"]),
            "proposals": len([j for j in self._judged if not j["exclude"]]),
            "approved": len(chosen), "demoted": len(self._demoted),
            "failed": self._failed, "spent_tokens": self._spent,
            "avg_ms": (sum(j["usage"].get("ms", 0) for j in self._judged
                           if not j["cached"])
                       // max(1, sum(1 for j in self._judged if not j["cached"]))),
            "cache_hits": sum(1 for j in self._judged if j["cached"]),
            "commit": out["commit"], "budget": params.budget_tokens,
        }
        out["report"] = await workflow.execute_activity(
            "report", stats, **_LLM_OPTS)
        out["spent_tokens"] = self._spent
```

- [ ] **Step 3: worker.py llm 분기 activities에 `judge_mod.report` 추가. 테스트에 `fake_report` 스텁 추가(경로 문자열 반환), Worker 등록.**

```bash
uv run pytest -q
```

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "Phase 4: report activity — 사이클 요약·비용 집계"
```

---

### Task 11: 주 1회 Temporal Schedule + CLI 핸들 해석

**Files:**
- Modify: `jobscouter/worker.py`
- Test: `tests/test_workflow.py` (핸들 해석 케이스)

**Interfaces:**
- Consumes: `JobScoutCycle`, `CycleParams`
- Produces:
  - Temporal Schedule `job-scout-weekly` — 매주 월 09:07 KST 사이클 자동 시작, 이전 사이클 진행 중이면 skip. **스케줄 시작 워크플로는 id에 타임스탬프가 붙으므로** signal/query CLI는 고정 id 대신 실행 중 워크플로를 찾는다.
  - `worker._running_handle(client) -> WorkflowHandle` — status/browser-done/approve가 사용
  - CLI `schedule` 서브커맨드 (1회 실행으로 스케줄 등록)

- [ ] **Step 1: worker.py — 핸들 해석 교체**

```python
async def _running_handle(client: Client):
    """실행 중인 사이클 핸들. 스케줄 시작 워크플로는 id가 매번 달라 검색으로 찾는다."""
    async for wf in client.list_workflows(
            "WorkflowType='JobScoutCycle' AND ExecutionStatus='Running'"):
        return client.get_workflow_handle(wf.id)
    sys.exit("실행 중인 사이클이 없다")
```

`status`/`browser-done`/`approve` 분기의 `client.get_workflow_handle(WORKFLOW_ID)`를 전부 `await _running_handle(client)`로 교체한다. 수동 `run`은 고정 id 유지(스케줄 id와 충돌 없음 — 스케줄은 타임스탬프를 덧붙인다).

- [ ] **Step 2: schedule 서브커맨드**

```python
    elif cmd == "schedule":
        from temporalio.client import (Schedule, ScheduleActionStartWorkflow,
                                       ScheduleOverlapPolicy, SchedulePolicy,
                                       ScheduleSpec)
        from jobscouter.workflow import JobScoutCycle
        await client.create_schedule(
            "job-scout-weekly",
            Schedule(
                action=ScheduleActionStartWorkflow(
                    JobScoutCycle.run, CycleParams(),
                    id=WORKFLOW_ID, task_queue=Q_WF),
                spec=ScheduleSpec(cron_expressions=["7 9 * * 1"],
                                  time_zone_name="Asia/Seoul"),
                policy=SchedulePolicy(overlap=ScheduleOverlapPolicy.SKIP),
            ))
        print("스케줄 등록: 매주 월 09:07 KST — 진행 중 사이클 있으면 skip")
```

- [ ] **Step 3: 테스트 — 핸들 해석**

`tests/test_workflow.py`에 추가 (기존 time-skipping env 재사용):
```python
@pytest.mark.asyncio
async def test_running_handle_finds_cycle():
    env = await WorkflowEnvironment.start_time_skipping()
    try:
        q = f"test-{uuid.uuid4()}"
        async with Worker(env.client, task_queue=q, workflows=[JobScoutCycle],
                          activities=[fake_run_script, fake_load_targets,
                                      fake_fetch_requirements, fake_judge,
                                      fake_search_context, fake_commit_rows,
                                      fake_report]):
            import jobscouter.workflow as wf
            wf._IO_OPTS["task_queue"] = q
            wf._LLM_OPTS["task_queue"] = q
            started = await env.client.start_workflow(
                JobScoutCycle.run, CycleParams(browser_wait_minutes=1),
                id=f"wf-{uuid.uuid4()}", task_queue=q)
            from jobscouter.worker import _running_handle
            h = await _running_handle(env.client)
            assert h.id == started.id
            await started.signal(JobScoutCycle.browser_done, "ok")
            await started.signal(JobScoutCycle.approve, [])
            await started.result()
    finally:
        await env.shutdown()
```

```bash
uv run pytest -q
```

- [ ] **Step 4: 스케줄 등록 + 기존 launchd fetch 은퇴 (작업 머신)**

```bash
uv run python -m jobscouter.worker schedule
temporal schedule list --address <server-ip>:7233        # job-scout-weekly 확인
# 주 1회 fetch만 돌리던 기존 launchd는 은퇴 (스케줄이 전체 사이클로 대체):
launchctl bootout gui/$(id -u)/dev.<user>.job-scout
```
주: 스케줄이 월요일에 사이클을 시작하면 작업 머신 워커가 꺼져 있어도 서버에서 durable 대기하고, 워커가 붙는 순간 fetch부터 진행된다.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "Phase 4: 주 1회 Temporal Schedule + 실행 중 사이클 핸들 해석"
```

---

### Task 12: 실사이클 검증 + 운영 문서 (완료 게이트)

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: 전체 시스템
- Produces: DESIGN Phase 4 완료 판정 — **사람 개입이 signal 2회(browser-done, approve)로 수렴**하는 실사이클 1회.

- [ ] **Step 1: README.md — 운영 절차**

````markdown
# job-scouter

job-scout 수동 조사를 Temporal workflow로 승격한 판정 파이프라인. 설계: DESIGN.md.

## 운영 (작업 머신)

```bash
uv run python -m jobscouter.worker io    # 터미널 1 — workflow+io (자격증명 없음)
uv run python -m jobscouter.worker llm   # 터미널 2 — judge·report (.env의 키)
uv run python -m jobscouter.worker run [--budget 2000000] [--dry-run]
```

사이클 진행: fetch → 판정(예산 내, 초과분은 미점수 강등) → **[signal 1]**
브라우저 조사(`/job-scout` 수동 실행 후 `browser-done`) → **[signal 2]**
등재 승인(`status`로 proposals 확인 후 `approve id...` 또는 `approve --none`)
→ candidates.json 커밋 → refresh → build → 보고서(reports/{날짜}_자동사이클.md).

```bash
uv run python -m jobscouter.worker status               # 단계·proposals·비용
uv run python -m jobscouter.worker browser-done "메모"
uv run python -m jobscouter.worker approve j54800311 365172
```

서버: 서버 OrbStack 컨테이너 `temporal` (deploy/compose.yaml, SQLite 볼륨 영속,
UI http://<server-ip>:8233). 작업 머신이 닫혀도 워크플로는 서버에서 대기하고,
워커를 다시 켜면 이어진다. **매주 월 09:07 KST에 Temporal Schedule
`job-scout-weekly`가 사이클을 자동 시작한다** (진행 중이면 skip —
`temporal schedule list --address <server-ip>:7233`).
설치·ES 개방: deploy/MINI_SETUP.md. 판정 캐시: data/judgments.jsonl
(공고id·루브릭버전·사실베이스해시 — 루브릭을 올리면 prompts/rubric_v2.md 추가
후 judge.RUBRIC_VERSION 변경, 전 건 자동 재판정 대상이 된다).

## 재색인 (판례·평판 갱신 시)

```bash
uv run python scripts/index_es.py
```
````

- [ ] **Step 2: 실사이클 1회 (실 커밋, 사용자와 함께) — BLOCKING**

```bash
uv run python -m jobscouter.worker run --budget 500000
```
사용자에게 signal 2회 시점을 안내: ① `/job-scout` 브라우저 조사 후 `browser-done`, ② `status`의 proposals 검토 후 `approve`. 완료 후 확인:
- candidates.json에 승인 행 추가 + ${DATA_REPO} repo 커밋 1개
- build.py 통과(거부 시 행 형식 버그 — 수정 후 재실행)
- reports/{날짜}_자동사이클.md 생성
- UI 타임라인에서 signal 2회·activity 재시도 이력 확인

- [ ] **Step 3: Commit + 완료 보고**

```bash
git add README.md && git commit -m "Phase 4: 운영 문서 + 실사이클 검증"
```
사용자에게 phase별 완료 판정 대비표(DESIGN §Phase)와 함께 종료 보고.

---

## Self-Review 결과 (작성 시 수행, 2026-08-24 개정 반영)

- **Spec coverage**: DESIGN §아키텍처(큐 3개·signal 2개·캐시키·예산 강등) → T1~T5·T9. §배포(서버 OrbStack·SQLite 볼륨·워커 전환 — 2026-08-24 launchd→OrbStack 변경) → T2. §데이터 경계(PKB 인덱스 불가침·llm 큐만 API·LAN 전용) → Global Constraints + T4 격리 테스트 + T7 자기 인덱스만 생성. Phase 2 게이트(20건 대조) → T6. Phase 3(ES 3종·하이브리드·측정 분리) → T7~T8. Phase 4(승인 커밋·report·주 1회 스케줄) → T9~T12. 루브릭 버전 스탬프·혼재 거부 → T4·T9.
- **의도적 범위 제외** (DESIGN과의 차이, 근거 있음): ① judge agent loop의 max_turns — 단일 forced-tool 호출이라 turn=1, 도구 loop 도입 시 추가. ② 평판 rep 배열 자동 기입 — 반어법 오탐 이력으로 사람 영역 유지, rep=null+사유. ③ OTel/Prometheus — usage 레코드로 시작(DESIGN 채택안 그대로). ④ 이력서(사실베이스·JK.md)의 PKB 기반 주기 갱신 — Phase 4 완료 후 별도 브레인스토밍·설계로 진행(2026-08-24 사용자 결정): "PKB 검색 → 변경 제안 diff → 사람 승인" 구조가 전제.
- **Type consistency**: Target/JudgeInput/Judgment는 config.py 단일 정의. workflow 안에서는 dict 접근(문자열 activity 호출) — T5 주석으로 명시. `search_context_text`(함수)와 `search_context`(activity) 구분 일관.
